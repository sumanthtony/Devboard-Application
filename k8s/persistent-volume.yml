---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  labels:
    app: devboard-postgres
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: /mnt/devboard-postgres